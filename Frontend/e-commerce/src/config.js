// El valor __API_URL__ es un placeholder que el contenedor del frontend
// reemplaza al arrancar (ver docker-compose.yml -> service frontend) con la
// URL real del API Gateway desplegado por CDK en LocalStack.
const API = '__API_URL__';

export default API;
